# Dependencies for the Hugging Face Space. Gradio itself is installed by the
# Space image according to `sdk_version` in the README frontmatter; pinning the
# full lock here conflicts with the image's own gradio extras, so only direct
# dependencies are listed. Local development uses pyproject.toml/uv.lock.
pandas>=2.3,<3
plotly>=6.7
